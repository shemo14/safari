import axios from "axios";import CONST from "../consts";import {Toast} from "native-base";export const setRate = (lang, service_id, token, rate) => {	return (dispatch) => {		axios({			url         : CONST.url + 'rateService',			method      : 'POST',			data        : { lang, service_id, rate},			headers		: token ? { Authorization: token } : null		}).then(response => {			// dispatch({type: 'setFav', payload: response.data});			Toast.show({				text        	: response.data.message,				type			: response.data.success ? "success" : "danger",				duration    	: 3000,				textStyle   	: {					color       	: "white",					fontFamily  	: 'ArbFONTS',					textAlign   	: 'center'				}			});		});	}};