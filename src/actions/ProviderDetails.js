import axios from "axios";import CONST from "../consts";export const getProviderDetails = (lang, provider_id, token) => {	return (dispatch) => {		axios({			url         : CONST.url + 'providerServices',			method      : 'POST',			data        : { lang, provider_id },			headers		: token ? { Authorization: token } : null		}).then(response => {			dispatch({type: 'getProviderDetails', payload: response.data});		});	}};