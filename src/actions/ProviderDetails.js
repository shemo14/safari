import axios from "axios";import CONST from "../consts";export const getProviderDetails = (lang, provider_id, token, lat, lng) => {	return (dispatch) => {		console.log('fuck you')		axios({			url         : CONST.url + 'providerServices',			method      : 'POST',			data        : { lang, provider_id, lat, lng },			headers		: token ? { Authorization: token } : null		}).then(response => {			dispatch({type: 'getProviderDetails', payload: response.data});		});	}};