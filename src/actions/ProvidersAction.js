import axios from "axios";import CONST from "../consts";export const getProviders = (lang, subCategories, lat , lng) => {	return (dispatch) => {		axios({			url         : CONST.url + 'ProvidersBySubCategory',			method      : 'POST',			data        : { lang, sub_category_id: subCategories, lat, lng }		}).then(response => {			dispatch({type: 'getProviders', payload: response.data});		});	}};